/**
 * まだ出さない機能のスイッチ。
 *
 * ここに並ぶのは「作りかけだから隠す」ものではなく、
 * **出すと嘘になるから隠す**ものだけ。
 */

/**
 * みんなの投稿・発見・通知(feed / discover / notifications /
 * post/$postId / u/$userId)。
 *
 * ## なぜ隠すか
 * この5画面は互いにリンクし合っているだけで、**アプリのどこからも辿り
 * 着けない**(下タブにも無い)。そのうえ `createPost` には呼び出し元が
 * 1つも無いので、誰も投稿できない。つまりURLを直接打った人だけが、
 * 永久に空のタイムラインに行き着く。
 *
 * さらに通報・ブロック・モデレーションが1つも無い。他人の投稿が並ぶ画面を
 * その状態で開けておくのは、単に未完成なのではなく**危ない**。
 *
 * 中途半端に見えているより、まだ無いほうがいい。出すと決めたときに、
 * 通報・ブロック・利用規約への同意をまとめて作ってからここを true にする。
 * 画面のコードはそのまま残してあるので、切り替えは1行で足りる。
 */
export const SOCIAL_ENABLED = false;

/**
 * 日記は実装と保存済みデータを残したまま、現在のアプリからは隠す。
 * 再公開するときはホームと専用ページが同時に戻る。
 */
export const JOURNAL_ENABLED = false;

/**
 * 図鑑の本棚表示は実装を残したまま選択肢から外す。
 * 写真・一覧・地図・カレンダーは引き続き使える。
 */
export const DEX_SHELF_ENABLED = false;
